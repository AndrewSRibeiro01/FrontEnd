# URL do backend NestJS (com o prefixo /api)
VITE_API_URL=http://localhost:3000/api

# Quando "true", o app usa os repositórios em memória (mocks).
# Quando "false", chama a API real definida em VITE_API_URL.
VITE_USE_MOCKS=true
